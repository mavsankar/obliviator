pushing after changes:-

git add <filename>
git commit -m "comment"
git push -u origin master

pulling latest code base:-

git pull <remote URL> <branch> or git pull origin master

git pull https://github.com/YashovardhanSiramdas/COM209P master

git pull https://github.com/OpenGenus/cosmos master

new repo:-

git init
git add -A
git commit -m "first commit"
git remote add origin <remote git>
git push -u origin master

new repo pulling and working:-

git init
git remote add origin <remote git>
git pull <remote URL> <branch>
w
grp wrking:-

git pull
do changes
git add -A
git commit
git push

rebase/sync:-

git remote add upstream https://github.com/whoever/whatever.git (1t)(actual url frm which repo is forked)

git fetch upstream

git checkout master

git rebase upstream/master

git push -f origin master  (-f for 1t,else -u)

new branch:-
git checkout -b new-branch existing-branch

making brancha equal to branch b:-
git checkout brancha
git reset --hard branchb
git push -f origin brancha

del branch:-
git push -d <remote_name> <branch_name> or git push <remote_name> --delete <branch_name>     //remotely
git branch -D branch_name   //locally

